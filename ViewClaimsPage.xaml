<Page x:Class="ST10039248_PROG6212.ViewClaimsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ST10039248_PROG6212"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewClaimsPage">

    <Grid Background="{StaticResource PrimaryColor}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10">
            <TextBlock Text="View Claims" Foreground="{StaticResource TextColor}" FontSize="24"/>
            <ListView Name="ClaimsList" Background="{StaticResource SecondaryColor}" Foreground="{StaticResource TextColor}">
                <!-- Bind claims data here -->
            </ListView>
        </StackPanel>
    </Grid>
</Page>
